const buildFailed = require('./types/buildFailed');
const buildSuccess = require('./types/buildSuccess');
const buildTerminated = require('./types/buildTerminated');
const custom = require('./types/custom');

module.exports = {
    buildFailed,
    buildSuccess,
    buildTerminated,
    custom,
};
